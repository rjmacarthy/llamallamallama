const config = {
  content: ['./src/**/*.{html,js,svelte,ts}'],

  theme: {
    extend: {}
  },

  variants: {
    extend: {
      opacity: ['disabled']
    }
  },

  plugins: []
}

module.exports = config
